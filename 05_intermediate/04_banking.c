#include <stdio.h>
#include <string.h>

void encrypt_password(char *password)
{
}

int main()
{
    char password[100];
    printf("Enter your password: ");
    scanf("%s", password);

    encrypt_password(password);

    return 0;
}
